# Legacy bash config (for the Bash CLI only).
# Copy to ~/.config/theme-manager/config or ./.theme-manager.conf
# For Rust, use config.toml.example as a template.
# Lines beginning with # are comments. Values are KEY=VALUE.

# Precedence: CLI flags > env vars > local config > user config > defaults.

# Root directory containing Omarchy themes.
THEME_ROOT_DIR="$HOME/.config/omarchy/themes"

# Symlink used to point to the current Omarchy theme.
CURRENT_THEME_LINK="$HOME/.config/omarchy/current/theme"

# Optional: prepend Omarchy's bin directory to PATH so omarchy-* commands resolve.
# OMARCHY_BIN_DIR="$HOME/.local/share/omarchy/bin"

# Waybar config directory (config.jsonc/style.css live here).
WAYBAR_DIR="$HOME/.config/waybar"

# Waybar themes directory (named theme folders live here).
WAYBAR_THEMES_DIR="$HOME/.config/waybar/themes"

# Waybar apply mode:
# - "copy" → copy config/style into WAYBAR_DIR and restart Waybar
# - "exec" → restart Waybar with -c/-s flags (uses tmplus-restart-waybar; falls back to copy if missing)
WAYBAR_APPLY_MODE="exec"

# Optional override for Waybar restart command (used when WAYBAR_APPLY_MODE="exec").
# WAYBAR_RESTART_CMD="tmplus-restart-waybar"

# Starship config file (active config lives here).
STARSHIP_CONFIG="$HOME/.config/starship.toml"

# Starship themes directory (named theme files live here).
STARSHIP_THEMES_DIR="$HOME/.config/starship-themes"

# Default Waybar behavior when -w/--waybar is not provided:
# - "auto"  → use the current theme's waybar-theme/ if present
# - "named" → use DEFAULT_WAYBAR_NAME from WAYBAR_THEMES_DIR
# - ""      → do nothing
DEFAULT_WAYBAR_MODE="auto"

# Name of a Waybar theme to use when DEFAULT_WAYBAR_MODE="named".
DEFAULT_WAYBAR_NAME=""

# Default Starship behavior when no selection is provided:
# - "preset" → use DEFAULT_STARSHIP_PRESET via `starship preset`
# - "named"  → use DEFAULT_STARSHIP_NAME from STARSHIP_THEMES_DIR
# - ""       → do nothing
DEFAULT_STARSHIP_MODE=""

# Name of a Starship preset when DEFAULT_STARSHIP_MODE="preset".
DEFAULT_STARSHIP_PRESET=""

# Name of a Starship theme when DEFAULT_STARSHIP_MODE="named".
DEFAULT_STARSHIP_NAME=""

# Set to 1 to default to quiet mode (same as -q/--quiet).
QUIET_MODE_DEFAULT=""
